server:
  port: 8080

spring:
  application:
    name: wayfarer-task-request

  datasource:
    url: jdbc:mariadb://localhost:3306/test
    username: username
    password: password
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

minio:
  endpoint: http://localhost:9000
  accessKey: aUhlqcifrjWPF6GG1Hp2
  secretKey: G7QqiGFYJoUbEJ2BhtdaozHEctwaEf9tyXKWRPed
  bucketName: test

server:
  port: 8080

spring:
  application:
    name: wayfarer-task-request

  datasource:
    url: jdbc:mariadb://localhost:3306/test
    username: username
    password: password
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

minio:
  endpoint: http://localhost:9000
  accessKey: EJEtF6JdhDtqH7RPc2xp
  secretKey: aOXQTuCzjzTlnVSapf7gdtDJdnAIqXQoFx22AWbw
  bucketName: test
